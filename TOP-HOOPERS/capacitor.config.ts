import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hoopers.app',
  appName: 'TOP-HOOPERS',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
